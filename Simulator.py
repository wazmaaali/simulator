import Events as Events

class Simulator:
    def __init__(self):
         self.generate_events(100)
        
    def generate_events(self,num_events):
        for i in num_events:
            pkt_arr_event = Events(arrival_time)
            pkt_dep_event = Events(dep_time)
            
            
            
          